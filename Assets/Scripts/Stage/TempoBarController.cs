using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TempoBarController : MonoBehaviour
{
    /*
    public static float Speed = 5f; // Tempo Bar 이동 속도
    private ObjectPool objectPool; // 오브젝트 풀
    private float despawnPosition = -30f; // 화면 밖으로 나가면 반환할 위치

    void Start()
    {
        objectPool = FindObjectOfType<ObjectPool>();
    }

    void Update()
    {
        // 이동
        transform.position += Vector3.back * (Speed * Time.deltaTime);

        // 화면에서 벗어나면 반환
        if (transform.position.z < despawnPosition)
        {
            objectPool.ReturnObject(gameObject);
        }
    }
    */
}